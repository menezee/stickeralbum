import React from 'react'

const style = {
    display: 'flex',
    height: '300px',
    backgroundColor: 'grey',
    alignItems: 'center',
    justifyContent: 'center'
}

export const Header = props => (
    <div style={style}>
        STICKER ALBUM
    </div>
)