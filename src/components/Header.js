import React from 'react'

const style = {
    display: 'flex',
    height: '300px',
    backgroundColor: 'grey',
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'column'
}

export const Header = () => {
    return (
        <div style={style}>
            STICKER ALBUM
        </div>
    )
}
